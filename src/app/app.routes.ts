import { Routes } from '@angular/router';
import { SendOtpComponent } from './send-otp/send-otp.component';

export const routes: Routes = [
    { path: '', component: SendOtpComponent },
    //TODO: Add route for verify otp component
];
